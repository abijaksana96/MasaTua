<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="20dp"
    android:layout_marginTop="16dp"
    android:background="@drawable/background_card_red"
    android:padding="20dp">

    <!--TODO : Ubah "User Profile" jadi bahasa indonesia-->

    <!-- Label "User Profile" di pojok kiri atas -->
    <TextView
        android:id="@+id/tv_user_profile_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="User Profile"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Foto Profil Bulat -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/iv_profile_picture"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginTop="8dp"
        android:background="#DEDEDE"
        android:padding="4dp"
        android:src="@drawable/ic_person"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_user_profile_label"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.App.CircleImageView"
        app:tint="#8A8A8A" />

    <!-- Label "Tambah foto" di bawah gambar -->
    <TextView
        android:id="@+id/tv_add_photo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:gravity="center"
        android:text="Tambah\nfoto"
        android:textColor="@android:color/white"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="@id/iv_profile_picture"
        app:layout_constraintStart_toStartOf="@id/iv_profile_picture"
        app:layout_constraintTop_toBottomOf="@id/iv_profile_picture" />

    <!-- Grup untuk Nama dan Email, dibuat agar bisa di-center-kan secara vertikal -->
    <LinearLayout
        android:id="@+id/user_info_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="@id/iv_profile_picture"
        app:layout_constraintEnd_toStartOf="@id/iv_edit_icon"
        app:layout_constraintStart_toEndOf="@id/iv_profile_picture"
        app:layout_constraintTop_toTopOf="@id/iv_profile_picture">

        <TextView
            android:id="@+id/tv_user_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/palet13"
            android:textSize="22sp"
            android:textStyle="bold"
            tools:text="Tasdik" />

        <TextView
            android:id="@+id/tv_user_email"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            tools:text="Tasdikganteng@gmail.com" />
    </LinearLayout>


    <!-- Ikon Edit di pojok kanan, di-center-kan secara vertikal dengan keseluruhan kartu -->
    <ImageView
        android:id="@+id/iv_edit_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_edit"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@android:color/white" />

</androidx.constraintlayout.widget.ConstraintLayout>
